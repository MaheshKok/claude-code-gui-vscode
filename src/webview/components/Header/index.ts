/**
 * Header Components Module
 *
 * This module provides the main header component for the application,
 * including navigation controls and status indicators.
 *
 * @module components/Header
 */

// Main export - Header is the primary component of this module
export { Header } from "./Header";

// Default export for convenient importing
export { default } from "./Header";

// Re-export types if needed by consumers
export type { HeaderProps } from "./Header";
